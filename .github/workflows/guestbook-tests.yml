name: Retro guestbook checks
on:
  pull_request:
    paths:
      - 'retro-1980/**'
      - 'supabase/**'
      - 'scripts/import-guestbook.js'
      - 'tests/**'
      - 'package*.json'
      - '.github/workflows/guestbook-tests.yml'
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm test
